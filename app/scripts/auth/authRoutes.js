

(function () {
    'use strict'
    angular.module('auth', ['ui.router'])
        .config(function ($stateProvider, $urlRouterProvider) {
            $stateProvider
                /*=auth*/

        });
})();