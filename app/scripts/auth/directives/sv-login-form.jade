.card(class='mb')
    .bgc-grey-200(layout-gt-sm='row' layout-sm="column" layout-align-gt-sm='space-between center' class='pt+ pb+ pl pr')
        .fs-title.mb.tc-grey-700
            i.mdi.mdi-account &nbsp
            | Log In
        sv-form-social-buttons
    div(class='pl pr' layout="row")
        div(flex="")
            form.mt(name="loginForm"  novalidate sv-form-val)
                md-input-container(class='')
                    label {{'email'|translate}}
                    input(name="email" type='text', ng-model='user.email' required)
                md-input-container(class='')
                    label {{'password'|translate}}
                    input(name="password" type='password', ng-model='user.password' required)
                div(layout='row' layout-align-sm="space-between center")
                    div(class='m')
                        button.btn.btn--s.btn--raised.btn--blue.bgc-blue-A200(lx-ripple ng-click='singIn()' id="svetLoginBtn")
                            i.mdi.mdi-login &nbsp
                            | {{loginBtn|translate}}
                    div(class='m')
                        sv-forgot-pass
        div(flex="" class='pl+')
            .fs-title.mt.tc-blue-600 Facebook or Google Plus will ask you for:
            .fs-subhead 1. Access to your basic account information
            p.p We only use your name and email address to create your account. We won’t use your list of friends unless you ask us to.
            .fs-subhead 2. Email
            p.p We will only send emails if you specifically sign up on our site.
            p.p We will never post anything to your wall or timeline unless you share it yourself.
    div.text-center.mt.bgc-grey-100.pb.pt(layout="row" layout-align="center center")
        span.fs-subhead.tc-grey-700 Don't have an account?
        a.btn.btn--s.btn--white.btn--raised.tc-red-600.m(lx-ripple='' ui-sref='app.sign-up') Please Sign Up

